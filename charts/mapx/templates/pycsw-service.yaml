apiVersion: v1
kind: Service
metadata:
  name: mx-meta-{{ .Release.Name }}
  labels:
    app: meta
spec:
  selector:
    app: meta
  ports:
    - name: default
      protocol: TCP
      port: 8000
      targetPort: 8000
