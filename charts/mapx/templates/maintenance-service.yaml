apiVersion: v1
kind: Service
metadata:
  name: mx-maintenance-{{ .Release.Name }}
  labels:
    app: maintenance
spec:
  selector:
    app: maintenance
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http