apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: meta.{{ .Values.endPoint }}
  labels:
    app: meta
spec:
  secretName: meta.{{ .Values.endPoint }}
  issuerRef:
    name: {{ .Values.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
  - meta.{{ .Values.endPoint }}
